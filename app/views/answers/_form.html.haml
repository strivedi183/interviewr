= form_for @answer, :remote => true do |f|
  = f.label   :name
  = f.text_field :name
  = f.label 'correct answer?'
  = f.check_box :is_correct
  = hidden_field_tag :question_id, @question.id
  = f.submit nil, :class => 'small button submit'