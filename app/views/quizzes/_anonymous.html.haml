%script{:src => "https://checkout.stripe.com/v2/checkout.js",
        :class => "stripe-button",
        :'data-label' => quiz.name,
        :'data-amount' => quiz.cost * 100,
        :'data-key' => Rails.configuration.stripe[:publishable_key]}
= form_tag purchase_quiz_path(quiz), :remote => true do end